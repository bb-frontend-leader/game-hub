# Base URL of the backend REST API (no trailing slash needed either way).
# Ask the backend team for the real value, then copy this file to .env.local
# (already git-ignored) or .env and fill it in. Example:
# VITE_API_URL=https://api.example.com
VITE_API_URL=
